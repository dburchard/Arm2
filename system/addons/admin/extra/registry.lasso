<?lassoscript

	define Arm_Registry => type {

		data public addon_registry = map(

		// insert below here
			'test' = map(),
			'pilot' = map(),
		// insert above here

		)

		public load() => {
		}

	}


?>